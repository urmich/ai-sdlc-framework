# AI SDLC Framework - Test Design / Test Plan

| Field | Value |
| --- | --- |
| Status | Implementation candidate; maintained as the living validation plan |
| Current phase | Candidate validation; publication and DEV remain separately authorized |
| Validates | [Requirements](requirements.md), FR-001 to FR-053 |
| Bound by | [Technical Design](technical-design.md) |
| Advancement to Technical Design | Authorized by the user on 2026-09-07 |
| Advancement to Coding | Authorized by the user |

This is one living document, not separate Test Design and Test Plan files.

## 1. Approach and limits

Keep validation short: observe this framework's own development and run small
deterministic checks over files, Git metadata, and CLI contracts. No separate
product, simulated development project, scripted AI sessions, model-judged
scoring, or benchmark harness is created.

The approved approach maps every requirement to checkpoints without exhaustive
per-DoD-condition evaluation. That limited depth is a project-specific exception
under FR-006, not a weaker default for projects using the framework. Manual
observation is deliberately lightweight; automate structural assertions where
they are actually deterministic.

Following the methodology before the framework is installed is useful design
feedback, not proof its eventual implementation works. Post-installation
observations and automated results are recorded separately from that feedback.
Coverage below means planned coverage, never that every requirement has passed.

## 2. Execution and evidence

Observe T-01..T-13 when their situations arise during this repository's work.
Do not invent a project, perform a real deployment, or manufacture an unrelated
bug just to exercise a checkpoint. Record unobserved behavior as `NotRun`.

T-14..T-52 use isolated temporary file trees, small event/hook-payload fixtures,
and minimal Git repositories containing only metadata/sample artifacts.
These are component fixtures, not simulated product work. They require no LLM
calls, cloud resources, live credentials, or modification of this checkout's
runtime state. Persistence failures are injected at specific steps rather than
using random kill timing. Never delete a live checkpoint for a test.
Deployment-flow cases use recorded-shape CI/CD provider response fixtures and a fake clock:
no actual pipeline runs, cloud deployments, or 60-second sleeps are needed.
PR cases use provider-response fixtures; no actual PR, review vote, or merge
is created by these tests. The formal candidate-review case uses the supported
Copilot CLI `/review` result contract or a deterministic fixture of that result;
it does not create a second review agent or an AI-evaluation benchmark.

For an executed checkpoint, record outcome, artifact/revision, environment,
validation cycle, test-specification identity, and a short evidence reference.
Keep logs outside this table. Every test's Status is `NotRun`, `Passed`, or
`Failed`, as required by FR-015. `NotRun` means no conclusive current-cycle
result is available; running activity, blockers, and unverified outcomes must be
stated separately rather than hidden behind that value.
Implementation readiness is also separate. The statuses below reflect actual
framework observations and deterministic executions for the current candidate.
Update them only from actual execution/user results; a new validation cycle resets
them to `NotRun` without deleting historical evidence.
Only local tests rerun automatically after a fix. DEV tests stay `NotRun` with
an awaiting-authorization detail until the user authorizes the separate DEV step.
Conformance verdicts and process exit codes are separate and tested by T-21.
In this framework's own plan, manual checkpoints are user observations and most
automated/semi-automated checks are agent/runner-owned. T-35 is a user-invoked
automated `/review` command. STAGING ownership and host restrictions are tested as
contracts, not by accessing a secured environment.

## 3. Checkpoints and coverage

| ID | Environment | Level | Checkpoint | Mode | Owner | Location | Requirements | Observe or assert | Implementation | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| T-01 | local | workflow | post-review | Manual | user | development-machine | FR-001, FR-002, FR-025 | Development intent activates Requirements without naming the framework; an information-only question does not. Initial canonical documents can be planned then created without Coding authority; protected metadata is rejected. A non-Git parent workspace permits safe Bash/PowerShell reads and captured-request repository bootstrap without requiring a new Copilot session. Follow-up work resumes the correct work item. | Framework-use observation: activation and resume | NotRun |
| T-02 | local | workflow | post-review | Manual | user | development-machine | FR-003, FR-011, FR-017, FR-020, FR-041 | Each non-overridden phase/stage boundary, including completion of candidate Review, requires user completion/advancement confirmation before automatic advancement or recommendation. A combined explicit reply is accepted without repeated prompts; readiness and pipeline completion are not consent. | Framework-use observation: decision boundaries | NotRun |
| T-03 | local | workflow | post-review | Manual | user | development-machine | FR-007, FR-008, FR-009, FR-010 | Requirements sources are not restricted to tickets; multiple items remain distinct; vague desired outcomes are clarified into per-requirement DoDs. | Framework-use observation: requirements analysis | NotRun |
| T-04 | local | workflow | post-review | Semi-automated | agent | local | FR-012, FR-013, FR-014, FR-016 | The agent plans and creates one canonical test document at the requested location, maps requirement coverage, assigns explicit execution checkpoints, and adds newly discovered test needs. | Agent procedure: canonical Test Plan maintenance | NotRun |
| T-05 | local | workflow | post-review | Manual | user | development-machine | FR-018, FR-019 | Technical Design addresses requirements and test impact without writing production or test implementation before authorization. | Framework-use observation: Technical Design boundary | NotRun |
| T-06 | local | workflow | post-review | Manual | user | development-machine | FR-021, FR-024, FR-041 | Every fix reruns the full required local suite from unit tests and then repeats `/review`. No DEV-specific build/deployment/test is triggered merely because local tests or Review passed; prior remote success and Review are historical until the changed candidate is revalidated. | Framework-use observation: per-fix validation loop | Failed |
| T-07 | local | workflow | post-review | Manual | user | development-machine | FR-015, FR-022, FR-023 | Execution modes match the plan; agent-driven flows need no user scenario steps; unexecuted/uncertain tests are not reported as passing. | Framework-use observation: test ownership and status | NotRun |
| T-08 | local | workflow | post-review | Semi-automated | agent | local | FR-025, FR-026 | Repository and branch/worktree setup works, documents/code are committed, local-only use remains valid, and pushes respect the user's authorization or explicit override. | Agent procedure: repository workflow exercise | Passed |
| T-09 | local | workflow | post-review | Manual | user | development-machine | FR-027, FR-031 | The installed framework and compatible settings are actually used; a later naturally occurring second-repository task needs no framework copy; conflicts require user resolution. | Framework-use observation: installed cross-repository behavior | NotRun |
| T-10 | local | workflow | post-review | Semi-automated | agent | local | FR-028, FR-030 | After resumption or compaction, the agent recovers the next authorized action before dependent mutations, without inventing progress or repeating uncertain operations. | Agent procedure: resume and reconciliation | NotRun |
| T-11 | local | workflow | post-review | Semi-automated | agent | local | FR-004, FR-029 | The agent automatically maintains in-scope earlier documents, reports each logical batch of changes, and uses meaningful Git history rather than a duplicate changelog. | Agent procedure: document maintenance and Git review | Passed |
| T-12 | local | workflow | post-review | Manual | user | development-machine | FR-005, FR-006 | Out-of-scope discoveries are surfaced. Execution, documentation, or both follow the user's explicit choice; neither permission implies the other. Scoped overrides are honored and truthfully reported without repeated challenges. | Framework-use observation: scope and override decisions | NotRun |
| T-13 | local | workflow | post-review | Manual | user | development-machine | FR-032, FR-033 | Progress and blockers are understandable; paused/completed, local-only, and explicitly overridden STAGING-only work receive applicable next actions. Promotion discovery is independent of event order and repository/target wildcard shape; new STAGING work is described as pending exact operation-policy evaluation rather than inferred from partial scope, while explicit/staged/implicit dispatched STAGING work remains reconcilable after consent revocation or expiry and takes precedence over an older terminal deployment. Failed/pending post-Review tests block completion advice; sources do not authorize actions; unavailable external permissions are respected. | Framework-use observation: status and permission boundaries | NotRun |
| T-14 | local | unit | pre-review | Automated | agent | local | FR-010, FR-012, FR-013, FR-014, FR-015, FR-028 | Phase-aware artifact/state checks require each test's Status to be NotRun/Passed/Failed, separate from readiness/blockers. IDs, DoDs, protected/planned locators, explicit checkpoints, due coverage and budgets resolve; validation preserves procedure changes in specification identity and rejects omitted/redefined definitions. | test/cli-install.test.mjs; test/review.test.mjs | Passed |
| T-15 | local | unit | pre-review | Automated | agent | local | FR-003, FR-006, FR-020, FR-029 | Audit replay accepts applicable events in the same commit or ancestors, retains scope/lifetime/revocation across repositories, preserves revocation tombstones and monotonic sequences, keeps legacy/archived retries readable and idempotent, and leaves retired history out of active state. | test/decisions.test.mjs | Passed |
| T-16 | local | unit | pre-review | Automated | agent | local | FR-028, FR-030 | At each write boundary, a process failure leaves recoverable old/new state. Newer artifact/deployment selections replace older projections deterministically, and the same artifact ID retains distinct DEV/STAGING selections. Record removal preserves target-before-revocation and event-before-audit ordering with bounded shared-graph traversal. A live old lock is not stolen; unique temporary files and serialized registry updates prevent lost writes. | test/storage.test.mjs; test/recovery-gate.test.mjs; test/deployment.test.mjs | Passed |
| T-17 | local | unit | pre-review | Automated | agent | local | FR-003, FR-006, FR-030, FR-032 | Captured receipt and snapshot bindings are required; fabricated free-form evidence is rejected. Reapplying the same decision is a no-op, conflicting reuse is rejected, and an effective event survives checkpoint loss. | test/decisions.test.mjs | Passed |
| T-18 | local | unit | pre-review | Automated | agent | local | FR-023, FR-024, FR-028, FR-030 | Missing handles and post-dispatch timeouts remain uncertain, never silently retried or pruned. Terminal operations, superseded current-cycle evidence, audited inactive authority and resolved blockers retire safely; repeated results/runs do not exhaust active limits. | test/recovery-gate.test.mjs; test/workflow.test.mjs; test/decisions.test.mjs | Passed |
| T-19 | local | integration | pre-review | Automated | agent | local | FR-025, FR-027, FR-028, FR-030 | Branch changes, secondary members, linked worktrees, missing registry/checkpoints, non-Git outer workspaces, and conflicting bindings recover the correct work item or block. A session-bound child repository is revalidated only when the outer path has no Git metadata; malformed/dubious metadata and another Git repository never inherit its authority. | test/storage.test.mjs; test/recovery-gate.test.mjs | Passed |
| T-20 | local | integration | pre-review | Automated | agent | local | FR-003, FR-004, FR-005, FR-006, FR-018, FR-020, FR-025, FR-026, FR-031, FR-034, FR-045 | The evaluator distinguishes local testing from live DEV targets; binds destination-specific push consent and the single effective push URL; identifies force/delete, multiple URLs, non-branch refs, parser discrepancies, escaping paths, outer-workspace execution, and stale/missing authority; preserves configured restrictions; recognizes safe bootstrap; and honors scoped overrides. Lifecycle-stage findings can become one-time advisories; other findings remain internal unmanaged results while the public hook falls through. | test/workflow.test.mjs; test/recovery-gate.test.mjs; test/review.test.mjs; test/cli-install.test.mjs | Passed |
| T-21 | local | unit | pre-review | Automated | agent | local | FR-012, FR-015, FR-024, FR-029 | Required unknown evidence exits 3, proven violations 2, checker errors 4. Success/not-applicable and explicit deviations are distinct in output; no missing test result becomes a pass. | test/cli-install.test.mjs | Passed |
| T-22 | local | integration | pre-review | Automated | agent | local | FR-028, FR-030, FR-045 | A session's first dependent mutation after compaction receives an orientation finding until its current token is acknowledged. Another session cannot clear that obligation; stale acknowledgements are rejected by framework state, but the hook does not block the user's tool action. | test/recovery-gate.test.mjs; test/workflow.test.mjs | Passed |
| T-23 | local | integration | pre-review | Automated | agent | local | FR-013, FR-027, FR-031, FR-032 | Install/update/uninstall respect custom Copilot home, paths with spaces, and existing user content. Authorized external artifact locations resolve correctly; unsafe/oversized record input is rejected explicitly, not silently truncated. | test/cli-install.test.mjs; test/storage.test.mjs | Passed |
| T-24 | local | integration | pre-review | Automated | agent | local | FR-023, FR-024, FR-026, FR-034, FR-041 | After local success, DEV-specific queue/test calls remain absent until current `/review` evidence, user Review completion, and DEV authorization exist. Then reuse/build the matching artifact, deploy, and test without per-job prompts. DEV needs no PR unless policy requires one; stale consent, failed builds, or decline do not advance. | test/deployment.test.mjs; test/workflow.test.mjs | Passed |
| T-25 | local | integration | pre-review | Automated | agent | local | FR-028, FR-030, FR-033, FR-035 | Monitor capability is checked before a framework trigger; values must be booleans and polling requires a real claimed worker. Every attached run polls at 60-second fake-clock intervals with origin-aware notices, terminal notification, gap/recovery handling, and explicit capability refresh. Reattachment alone cannot upgrade a blocked monitor. Wrong-run/login pages never count as verified links. | test/monitors.test.mjs | Passed |
| T-26 | local | integration | pre-review | Automated | agent | local | FR-003, FR-015, FR-031, FR-034, FR-036 | Local success or DEV authorization alone cannot skip DEV and enter STAGING. Authorized DEV testing and user-confirmed completion precede STAGING recommendation/consent. A combined explicit completion/promotion reply avoids redundant prompts and must bind the actual current DEV deployment unless a scoped completion override applies; stale, absent, or identity-free consent does not advance. | test/deployment.test.mjs; test/workflow.test.mjs | Passed |
| T-27 | local | integration | pre-review | Automated | agent | local | FR-014, FR-015, FR-022, FR-037 | Successful STAGING deployment generates policy-specific execution guidance using the configured owner and authorized location. User-run automated suites remain automated; agent/provider execution is allowed only by policy and normal operation authority. DEV/STAGING results and completion bind the current deployment; replacements invalidate older/downstream success, and late evidence from a superseded deployment cannot overwrite the current result. | test/deployment.test.mjs | Passed |
| T-28 | local | integration | pre-review | Automated | agent | local | FR-003, FR-006, FR-032, FR-038, FR-040 | After STAGING confirmation, the PROD recommendation reports current PR readiness. Missing, failed, pending, or stale validation produces prerequisite advice, not a ready claim, PR creation, or PROD queue. Candidate hashing remains bounded and stable across Git-normalized staging. | test/deployment.test.mjs; test/pr.test.mjs; test/review.test.mjs | Passed |
| T-29 | local | integration | pre-review | Automated | agent | local | FR-015, FR-024, FR-034, FR-036, FR-037, FR-041 | Repeated fixes after any environment rerun local tests from units and invalidate `/review`, but queue zero replacement DEV runs without authorization. Hook and resume recovery derive changed canonical Test Plan definitions instead of failing on the old cycle. Multiple locally reviewed candidates can precede one DEV attempt. Old Review/remote results/consent stay historical; status-only updates cause no reset. | test/deployment.test.mjs; test/workflow.test.mjs; test/recovery-gate.test.mjs | Passed |
| T-30 | local | integration | pre-review | Automated | agent | local | FR-001, FR-034, FR-035, FR-038 | Existing framework/CI/user-reported runs are monitored and completion is notified without duplicate queues. A successful CI artifact or observed user-triggered DEV run does not authorize new DEV work or grant stage completion. | test/monitors.test.mjs | Passed |
| T-31 | local | integration | pre-review | Automated | agent | local | FR-004, FR-005, FR-024 | In-scope maintenance sends change notices without approval; out-of-scope execution/documentation permissions and once-only reservations remain distinct. Documentation cannot consume execution authority or reuse one-shot documentation consent. An authorized fix in the candidate restarts local revalidation without automatically starting remote pipelines. | test/workflow.test.mjs | Passed |
| T-32 | local | integration | pre-review | Automated | agent | local | FR-003, FR-020, FR-025, FR-039, FR-041 | Explicit scoped PR authority selects the correct source/target and draft/ready state; an appropriate existing PR is reused. Once-only authority cannot publish a second operation and is not consumed by a separate prerequisite push. No unauthorized publication or duplicate after timeout occurs. An early document/draft PR is visibly unreviewed and does not grant Coding or Review completion. | test/pr.test.mjs | Passed |
| T-33 | local | integration | pre-review | Automated | agent | local | FR-015, FR-024, FR-035, FR-040, FR-041 | Required checks match the current PR source/target or proven merge context and their own exact verified monitor association. Missing, failed, pending, stale, unrelated, merely same-status, or another check's shared run association cannot satisfy PROD readiness. A previously standalone monitor can be immutably associated later without changing its origin. Changed candidate content returns to unit-first local revalidation and `/review` before another push. | test/pr.test.mjs | Passed |
| T-34 | local | integration | pre-review | Automated | agent | local | FR-026, FR-034, FR-036, FR-038, FR-039, FR-040, FR-041 | Environment policy conditionally requires a PR before the dependent artifact/deployment step, not before the PR's own validation. An authorized bundle avoids duplicate prompts; absent PR authority or candidate Review pauses publication. Green CI neither replaces `/review`, supplies an absent artifact, nor enables merge, auto-merge, policy bypass, or PROD execution. | test/pr.test.mjs | Passed |
| T-35 | local | review | review | Automated | user | development-machine | FR-003, FR-024, FR-039, FR-041 | After every `pre-review` test passes, the framework presents Copilot CLI `/review` and binds its receipt-confirmed active result to the candidate/spec/configuration. The event satisfies only the built-in Review checkpoint; independent Review/post-Review checks still gate completion. Revocation, expiry or a later failed prerequisite blocks publication. | GitHub Copilot CLI /review; test/review.test.mjs | Passed |
| T-36 | local | integration | pre-review | Automated | agent | local | FR-027, FR-042 | Repeated packaging produces exactly one metadata-derived `ai-sdlc-framework-<version>.tgz` with the same SHA-256 digest. Unsafe output directories and tampered archives fail without deleting source. Verification rebuilds expected identity, sizes, and file inventory from the same source, then compares and installs the distributable. The archive excludes tests/runtime/repository metadata, uses a Windows-safe npm invocation contract, verifies under restrictive Unix umask, passes doctor, updates idempotently, uninstalls owned files, and preserves seeded user instructions plus runtime content. Workflow assertions require public-registry package configuration, trusted publication with provenance, repository-identity validation, and publication of the same validated artifact to npm and GitHub Releases. Fixture-owned HOME, npm cache/config, offline mode, disabled update notification, and removed token variables prevent registry, credential, or real-home access. | test/package.test.mjs; test/npm-environment.mjs; scripts/package.mjs; scripts/verify-package.mjs; .github/workflows/ci.yml | Passed |
| T-37 | local | integration | pre-review | Automated | agent | local | FR-027, FR-031, FR-043 | Five substantive task-focused instruction files exist, avoid copied reference-repository-specific technologies, and cover required knowledge, coding, testing, building, and reviewing concepts. Global instructions and relevant phase skills reference them; install/update place them under `sdlc/instructions`, package verification includes them, and uninstall removes unchanged owned copies. | test/instructions.test.mjs; test/cli-install.test.mjs; scripts/check.mjs | Passed |
| T-38 | local | integration | pre-review | Automated | agent | local | FR-027, FR-028, FR-031, FR-044, FR-045 | Host-neutral contracts normalize documented hook aliases/field shapes; keep Bash/zsh, PowerShell, and cmd tokenization separate; classify ambiguous expansion/chaining/redirection/quoting as unsupported rather than trusted; select only platform/shell-compatible exact repository commands; and exercise simulated Windows drive, UNC, separator, case, line-ending, special-entry, process-invocation, Git-diagnostic, and doctor-capability behavior. Unsupported shell/path forms remain unmanaged and uncredited, while the public hook still falls through. | test/platform.test.mjs; test/recovery-gate.test.mjs; test/cli-install.test.mjs; test/package.test.mjs | Passed |
| T-39 | local | integration | pre-review | Automated | agent | macOS host | FR-027, FR-028, FR-044 | On the current macOS host, install/update/doctor/uninstall and package verification work in paths with spaces; native filesystem locks, atomic replacement, modes, symlink containment, process invocation, and Git binding pass. A native Bash/zsh argument observer proves classifier tokens and effective cwd for spaces, supported empty arguments, quoting, and trailing backslashes. If PowerShell Core is installed, its supported subset and explicit empty-argument rejection are supplemental syntax evidence, not native Windows proof. | test/platform.test.mjs; test/storage.test.mjs; test/cli-install.test.mjs; test/package.test.mjs; host evidence | Passed |
| T-40 | local | integration | pre-review | Automated | agent | Windows host | FR-027, FR-028, FR-044, FR-045 | On Windows, the npm or offline release package completes install/update/doctor/clean-migration/uninstall through both Windows PowerShell 5.1+ and cmd.exe. Native argument observers separately prove classifier argv and effective cwd for both shells, including spaces, supported/rejected empty arguments, quoting, and trailing backslashes. Native evidence also covers drive-letter and UNC roots, cmd-on-UNC remaining unsupported/unmanaged without a false cwd claim while the public hook falls through, separators/case including distinct per-directory case-sensitive siblings, CRLF/LF, NTFS modes/attributes, symlinks/junctions/reparse points, sharing locks and atomic replacement, Node/npm/Git invocation, worktrees, repository command adapters, and actual Copilot hook payloads. Unsupported privileges or capabilities are reported rather than converted to passes. | test/platform.test.mjs; test/storage.test.mjs; test/cli-install.test.mjs; test/package.test.mjs; Windows host evidence | NotRun |
| T-41 | local | integration | pre-review | Automated | agent | local | FR-006, FR-020, FR-025, FR-028, FR-031, FR-032, FR-045 | Phase, orientation, local-validation, candidate-Review, and stage-completion findings remain visible but, after a captured user direction, the public hook emits at most one advisory per session/rule set. Repeated stage overrides do not repeat the warning. Missing binding, out-of-scope classification, unknown/malicious shell syntax, path/configuration conflicts, arbitrary Node programs, destructive/external findings, state-integrity failures, malformed hook input, and missing runtime imports all produce unmanaged/fall-through hook output with no deny/ask decision. Internal evaluation remains available for truthful credit and diagnostics. | test/advisory-gate.test.mjs; test/recovery-gate.test.mjs; test/workflow.test.mjs | Passed |
| T-42 | local | integration | pre-review | Automated | agent | local | FR-027, FR-028, FR-044, FR-045 | Outside Git, direct literal `doctor`/`install`/`update`/`uninstall` calls to the installed entry or a receipt-selected framework source/package are recognized on Bash, PowerShell, and cmd with paths containing spaces. Chaining, expansion, redirection, alternate scripts, and arbitrary Node programs remain untrusted but non-blocking. An intact isolated version-1.2.0 installation updates to the current package, preserves runtime/unowned content, and current `doctor` reports the new version, installed true, and no findings. A modified-owned-file case preserves content/state and reports the conflict without claiming a clean upgrade. | test/advisory-gate.test.mjs; test/cli-install.test.mjs; test/package.test.mjs | Passed |
| T-43 | local | integration | pre-review | Automated | agent | local | FR-006, FR-027, FR-045 | Installed instructions and skills state that explicit user direction can override framework lifecycle stages after at most one warning, no framework finding makes the requested Copilot action impossible, real enforcement remains external, state rejection cannot fabricate managed credit, and Copilot must restart after install/update. Static checks protect the fail-open launcher and document host-terminal recovery when an already-running older hook blocks its own replacement. | test/instructions.test.mjs; test/advisory-gate.test.mjs; scripts/check.mjs | Passed |
| T-44 | local | integration | pre-review | Automated | agent | local | FR-001, FR-002, FR-006, FR-046 | Every development request receives one natural attempt to follow the applicable framework stage, while the user may override at any time. The reported framework-port-to-marketplace-plugin prompt, including guideline discovery, eventual PR/marketplace delivery, and “go,” starts Requirements and does not imply skipped stages. The opening proposes gathering requirements and begins discovery; later transitions explain Test Plan quality gates, Technical Design, Coding, and Review once. Only clear skip/bypass/reorder/reject-stage intent counts. If already present, the agent explains the skipped stage once and immediately honors it. An unrecorded stage advisory never binds managed operation credit; a receipt-bound explicit override remains supported. | test/instructions.test.mjs; test/advisory-gate.test.mjs | Passed |
| T-45 | local | integration | pre-review | Automated | agent | local | FR-003, FR-010, FR-012, FR-013, FR-014, FR-015, FR-019, FR-021, FR-027, FR-028, FR-030, FR-047 | Three member repositories can each register multiple Requirements, Test Plan, and Technical Design artifacts. Stable `(role, repositoryId, artifactId)` identity appends named documents and replaces only the matching document; omitted IDs retain legacy `default` behavior. Duplicate paths across IDs/roles are rejected deterministically. Status, checks, resume/orientation, approval snapshots, aggregate requirement/test coverage, combined Test Plan identity, per-plan status synchronization, and legacy single-repository/external locators include the complete applicable artifact set. Rebinding an external artifact to a same-content path invalidates pending approval. A one-plan legacy active cycle retains its digest/evidence after upgrade; adding another plan resets specification identity. | test/multi-repository-artifacts.test.mjs; test/decisions.test.mjs; test/recovery-gate.test.mjs | Passed |
| T-46 | local | integration | pre-review | Automated | agent | local | FR-027, FR-028, FR-044, FR-045, FR-048 | Running the new package's `install --purge-existing` first rejects an installed executing entry, incomplete replacement inventory, invalid package metadata, or unsafe purge parent without deleting prior state. It then holds one external lock while removing unchanged and modified framework-owned hooks, skills, instruction blocks, CLI/runtime state, manifests, locks, work items, decisions, and evidence and installing a healthy OSS framework. Tests cover corrupt manifests, runtime and complete-framework symlinks, unrelated targets, oversized/impossible abandoned locks, a newly populated live lock, competing installation, post-purge failure reporting, and `doctor`. Unrelated Copilot instructions/files remain intact, ordinary install/uninstall remain non-destructive, repeated purge is safe, and migration documentation uses a cross-platform npm/npx command. | test/cli-install.test.mjs; test/package.test.mjs | Passed |
| T-47 | local | integration | pre-review | Automated | agent | local | FR-017, FR-020, FR-025, FR-049 | Deployment-capable operations resolve to exactly one DEV/STAGING/PROD environment. Tests cover existing canonical stage inference, explicit environments with arbitrary allowed stage names, valid repository/provider/pipeline/target/configuration mappings, missing environments, unknown implicit labels, wrong-scope mappings, multiple mappings, explicit-versus-mapped conflicts, unauthorized stages, normalized prepared-operation records and authority reservation. Unresolved operations fail managed preparation with an `environment-resolution` finding while the advisory hook remains non-blocking for user-directed unmanaged execution. | test/workflow.test.mjs; test/advisory-gate.test.mjs | Passed |
| T-48 | local | integration | pre-review | Automated | agent | local | FR-035, FR-040, FR-050 | Monitoring uses complete provider-neutral execution identities and adapter-derived links. Tests cover identity-key separation by provider/connection/scope/definition/execution/attempt, Azure DevOps build metadata normalization, same-build redirects, wrong-build URLs despite matching caller context, inaccessible/malformed observations, missing and duplicate adapters, custom adapter registration, continued status polling after link failure, archived notices, and PR-check association using the complete identity. | test/provider-adapters.test.mjs; test/monitors.test.mjs; test/pr.test.mjs; test/package.test.mjs | Passed |
| T-49 | local | integration | pre-review | Automated | agent | local | FR-020, FR-046, FR-051 | One installed override-intent guide is the source used by the orchestrator and every lifecycle skill. A deterministic prompt matrix covers ordinary implementation/fix/urgency/publication/end-to-end language; ambiguous intent; negated skip language (“do not skip Requirements”); non-lifecycle uses of skip/bypass; and unmistakable skip/bypass/reorder/reject-stage instructions at each current stage. Contract assertions require ordinary/ambiguous cases to identify and begin useful current-stage work, explicit cases to produce one consequence warning and unmanaged continuation, and every case to avoid fabricated approval, completion, or override events. Tests verify all source and installed skill references resolve to the same guide rather than relying on divergent copied wording. | test/instructions.test.mjs; test/advisory-gate.test.mjs; test/cli-install.test.mjs | Passed |
| T-50 | local | integration | pre-review | Automated | agent | local | FR-015, FR-022, FR-037, FR-052 | STAGING configuration validates an execution contract containing owner (`user`, `agent`, `provider`, or `external-system`) and one or more authorized locations. Negative fixtures cover missing/invalid owner, empty locations, unauthorized location, unavailable tool path, failed operation preparation, absent/declined/wrong-scope fallback, and a fallback question without authorization; all assert no dispatch and no inferred handoff. Positive fixtures cover user handoff, agent execution, provider execution, external-system evidence, a revised contract, and an explicit correctly scoped fallback. All-Passed-but-unconfirmed and mixed Passed/Failed/NotRun fixtures for multiple owners independently assert truthful per-test results, pending STAGING completion, and no normal PROD recommendation; mismatched environment/deployment/artifact/Test Plan/cycle confirmation is rejected. Deterministic scans cover requirements, Test Plan guidance, global and focused engineering instructions, every lifecycle `assets/skills/**/SKILL.md`, templates, recovery, CLI docs, generated handoff/status text, and installed/updated copies. They reject universal dedicated-machine/user-owned assertions while allowing clearly policy-qualified examples. | test/deployment.test.mjs; test/workflow.test.mjs; test/instructions.test.mjs; test/cli-install.test.mjs | Passed |
| T-51 | local + CI matrix | integration | pre-review | Automated | agent | native Windows x64, macOS x64/arm64, Linux x64 | FR-027, FR-042, FR-044, FR-053 | **Prepublication release gate.** Two independent same-source/toolchain builds produce byte-identical npm and standalone archives plus one versioned checksum/payload manifest. Canonical inventory checks reject missing, extra, duplicate, changed, truncated, or wrong-platform entries and any difference outside allowlisted launchers, metadata, executable modes, and line endings. Every archive contains the license, version, checksum reference, payload, and required launcher. A positive isolated scenario blocks npm/network access while a verified standalone archive and Node 22+ are available, then completes install, doctor, update, ordinary uninstall, clean migration, and purge. Missing/malformed/swapped checksums, wrong-version artifacts, Node 21, missing/shadowed/non-executable/wrong-architecture Node, interrupted download/extraction/copy/replace, permission/sharing failures, simultaneous installs, install-versus-purge, and interrupted channel switching assert no premature execution, no unintended destructive migration, bounded locking/cleanup, preserved prior framework/unrelated state, and truthful partial-purge reporting. WinGet validation rejects stale URLs/versions, wrong installer/uninstall metadata, missing Node dependency, and architecture mismatch. Homebrew audit/style rejects stale URLs/versions, missing dependency, and swapped Intel/arm64 checksums. Package-manager install/upgrade/uninstall snapshots prove they affect only channel payloads until explicit `sdlc install`/`--purge-existing`; switching executes the new launcher before old-package removal and verifies hooks no longer depend on the old payload. Distinct release-blocking jobs assert actual process OS/architecture for Windows x64, macOS x64, macOS arm64, and Linux x64; failed, skipped, cancelled, or absent mandatory jobs prevent publication. Windows exercises WinGet metadata plus PowerShell/cmd lifecycle; macOS exercises standalone and real Homebrew install/upgrade/uninstall; Linux exercises standalone. Stable metadata excludes prereleases. | test/distribution-channels.test.mjs; test/package.test.mjs; scripts/package-platforms.mjs; scripts/verify-platform-package.mjs; installer smoke scripts; .github/workflows/ci.yml | NotRun |
| T-52 | public release | acceptance | post-review | Automated + manual | agent | anonymous clean clients + Microsoft-managed client | FR-027, FR-044, FR-053 | **Postpublication acceptance.** After authorized publication, clean clients anonymously fetch release metadata, checksum manifest, and exact candidate-bound assets with empty caches and no repository credentials; downloaded SHA-256 and payload manifest must equal the prepublication evidence before execution. Public npm install/update/uninstall/purge, direct standalone install, and real Homebrew tap install/upgrade/uninstall run against published bytes in clean homes. GitHub Release publication and Homebrew tap usability are required; WinGet remains `repository-ready` until community acceptance and becomes client-available only after a real `winget` discovery/install test. Channel removal leaves explicit Copilot-home installation intact; clean migration remains explicit. CFS quarantine, package exception, client registry denial, WinGet policy, GitHub/download denial, and OS execution controls are tested and reported separately. A generic network error never proves CFS, and no alternate channel is promised to bypass policy. Inconclusive execution remains Test Plan `NotRun` with a separate `Blocked` diagnostic. Documentation assertions require npm-first guidance, equivalent channel lifecycle commands, approved CFS wait/exception guidance, checksums-before-execution, restart requirements, and no bypass language. | CI post-publish consumer jobs; docs/ado-acceptance-test.md; published release evidence; native Windows acceptance result | NotRun |

## 4. Integration limits

| Area | What local tests establish | What remains unverified until exercised |
| --- | --- | --- |
| Hooks and user input | Rules for documented payload fixtures; advisory evaluation; fail-open command-hook output; supported installed CLI adapter smoke checks | Behavior of untested CLI versions or paths not intercepted |
| Cloud/pipeline work | Per-fix local restart, separate DEV consent, STAGING handoff, pipeline monitoring, and link/recovery rules through fixtures | Actual provider access, scheduler/tool connectivity, deployment pipelines, and user testing on secured STAGING machines |
| PR workflow | Creation/reuse, current-check provenance, prerequisite ordering, and separate permissions through fixtures | Actual provider publication, policy evaluation, and CI integration |
| Candidate Review | Candidate/result binding, stale-review invalidation, and publication/DEV gates using deterministic `/review` result fixtures | The semantic quality and completeness of the built-in `/review` analysis on arbitrary real code |
| Distribution CI/CD | Reproducible local packaging, integrity metadata, workflow contract, and isolated install/update/doctor/uninstall | A hosted GitHub Actions run and installation into the user's real Copilot home |
| Focused instructions | Dedicated guide structure, references, package/install ownership, and technology-neutral concept contracts | Semantic quality across arbitrary repositories and future technology ecosystems |
| Host platforms | Deterministic adapter/path contracts and native macOS behavior; PowerShell Core syntax evidence when available | Native Windows PowerShell 5.1, cmd.exe, NTFS drive/UNC/case/junction/lock/replace behavior, and actual Windows Copilot hook payloads until run on Windows |
| Multiple repositories | Work-item/member identity and recovery over small metadata fixtures | Full cross-repository development behavior in ordinary use |
| History | Record integrity and applicable authorization claims | When code was actually written, semantic intent, and completeness of absent history |

These limits do not remove requirements. Report them as unverified until there
is appropriate evidence. Do not create a separate project or expensive campaign
to hide the gaps.

## 5. Cost and design maintenance

The available development window is roughly one to one and a half days.
Deterministic cases stay small and run locally; manual review accompanies work
already happening. No AI evaluation suite is planned.

T-16..T-46 cover the design's concrete failure paths, deployment/PR requirements,
candidate Review, installable package CI, and focused engineering instructions.
The one-minute cadence is tested with virtual time, not long-running AI sessions.
These are focused contract cases, not extra product scope. Add or refine a short
case when implementation or observation exposes a new defect. Do not turn these
checkpoints into a project simulation or silently
reduce the required behavior to obtain green results.
